diff --git a/node_modules/@nomicfoundation/hardhat-ledger/dist/src/internal/hook-handlers/network.js b/node_modules/@nomicfoundation/hardhat-ledger/dist/src/internal/hook-handlers/network.js
index a622fa4..bae93fc 100644
--- a/node_modules/@nomicfoundation/hardhat-ledger/dist/src/internal/hook-handlers/network.js
+++ b/node_modules/@nomicfoundation/hardhat-ledger/dist/src/internal/hook-handlers/network.js
@@ -34,7 +34,13 @@ export default async () => {
             if (jsonRpcRequest.method === "eth_accounts") {
                 const accountsResponse = await next(context, networkConnection, jsonRpcRequest);
                 if (isFailedJsonRpcResponse(accountsResponse)) {
-                    return accountsResponse;
+                    // If the RPC node doesn't support eth_accounts,
+                    // return only the Ledger accounts instead of propagating the error.
+                    return {
+                        jsonrpc: "2.0",
+                        id: jsonRpcRequest.id,
+                        result: [...ledgerHandler.getLedgerAccounts()],
+                    };
                 }
                 assertHardhatInvariant(Array.isArray(accountsResponse.result) &&
                     accountsResponse.result.every((v) => typeof v === "string"), "accountsResponse.result should be an array and every element should be a string");
